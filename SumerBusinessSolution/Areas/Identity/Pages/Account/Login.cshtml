@page
@model LoginModel
@using Sumer.Utility
@using Microsoft.AspNetCore.Localization
@using System.ComponentModel.DataAnnotations;
@*@inject IHtmlLocalizer<IndexModel> htmlLocalizer*@ @*for html lozcalization*@
@*@inject IStringLocalizer<IndexModel> localizer*@
@*First Method*@
@*@inject Services.CommonLocalizationService localizer*@
@*Second and the easier Method*@


@{
    ViewData["Title"] = "Log in";
}

    <div class="  border backgroundWhite">
        <h2 class="text-info  text-md-right">تسجيل الدخول</h2>
        <form method="post" id="account" asp-route-returnUrl="@Model.ReturnUrl" enctype="multipart/form-data">
            <div asp-validation-summary="All" class="text-danger"></div>

            <div class="row form-group">
                <div class="col-md-5 offset-md-5">
                    <input asp-for="Input.Email" class="form-control text-md-right" />
                </div>
                <div class="col-md-2">
                    <label asp-for="Input.Email"></label>
                </div>

                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            @*<div class="form-group">
            <label asp-for="Input.Email"></label>
            <input asp-for="Input.Email" class="form-control" />
            <span asp-validation-for="Input.Email" class="text-danger"></span>
        </div>*@

            <div class="form-group row">
                <div class="col-md-5 offset-md-5">
                    <input asp-for="Input.Password" class="form-control text-md-right" />
                </div>
                <div class="col-md-2">
                    <label asp-for="Input.Password"></label>
                </div>
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>
            @*<div class="form-group">
            <label asp-for="Input.Password"></label>
            <input asp-for="Input.Password" class="form-control" />
            <span asp-validation-for="Input.Password" class="text-danger"></span>
        </div>*@

            <div class="form-group row">

                <div class="col-md-1 offset-md-9">
                    <div class="checkbox">
                        <input asp-for="Input.RememberMe" />
                    </div>
                </div>
                <div class="col-md-2">
                    <label asp-for="Input.RememberMe">
                        @Html.DisplayNameFor(m => m.Input.RememberMe)
                    </label>
                </div>

                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>

            @*<div class="form-group">
            <div class="checkbox">
                <label asp-for="Input.RememberMe">
                    <input asp-for="Input.RememberMe" />
                  
                </label>
            </div>
        </div>*@

            <div class=" form-group row">
                <div class="col-md-5 offset-md-5">
                    <div class="row">
                        <div class="col-md-6">
                           
                        </div>
                        <div class="col-md-6">
                            <input type="submit" asp-route-culture="@Request.RouteValues["culture"]" class="btn btn-info form-control" value="تسجيل دخول" />
                        </div>
                    </div>
                </div>
            </div>


            @*<div class="form-group">
                <button type="submit" class="btn btn-primary">تسجيل دخول</button>
            </div>*@
            <div class="form-group text-md-right">
                <p>
                    <a id="forgot-password" asp-page="./ForgotPassword">نسيت كلمة السر؟</a>
                </p>
                <p>
                    <a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl">تسجيل مستخدم جديد</a>
                </p>
                <p>
                    <button type="submit" asp-page-handler="SendVerificationEmail" class="btn-link" style="padding:0px;margin:0px;border:0px">اعادة ارسال تأكيد البريد الالكتروني</button>
                </p>
            </div>
        </form>
                
           
                @*<div class="col-md-6 col-md-offset-2">
                    <section>
                        <h4>Use another service to log in.</h4>
                        <hr />
                        @{
                            if ((Model.ExternalLogins?.Count ?? 0) == 0)
                            {
                                <div>
                                    <p>
                                        There are no external authentication services configured. See <a href="https://go.microsoft.com/fwlink/?LinkID=532715">this article</a>
                                        for details on setting up this ASP.NET application to support logging in via external services.
                                    </p>
                                </div>
                            }
                            else
                            {
                                <form id="external-account" asp-page="./ExternalLogin" asp-route-returnUrl="@Model.ReturnUrl" method="post" class="form-horizontal">
                                    <div>
                                        <p>
                                            @foreach (var provider in Model.ExternalLogins)
                                            {
                                                <button type="submit" class="btn btn-primary" name="provider" value="@provider.Name" title="Log in using your @provider.DisplayName account">@provider.DisplayName</button>
                                            }
                                        </p>
                                    </div>
                                </form>
                            }
                        }
                    </section>
                </div>*@
       
    </div>

        @section Scripts {
            <partial name="_ValidationScriptsPartial" />
        }
