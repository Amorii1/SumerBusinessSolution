@page
@model SumerBusinessSolution.Pages.Inventory.IncomingGoods.CreateModel
@using SumerBusinessSolution.Utility
@using Microsoft.AspNetCore.Localization
@using System.ComponentModel.DataAnnotations;
@*@inject IHtmlLocalizer<IndexModel> htmlLocalizer*@ @*for html lozcalization*@
@*@inject IStringLocalizer<IndexModel> localizer*@
@*First Method*@
@inject Services.CommonLocalizationService localizer
@*Second and the easier Method*@


@{
    ViewData["Title"] = "CreateIncomingGoods";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}
<partial name="_StatusMessage" model="Model.StatusMessage" />
<div class="  border backgroundWhite">
    @*<h2 class="text-info">المواد الواردة </h2>*@
    <h2 class="text-info text-md-right">  @localizer.Get("أضافة مواد واردة")</h2>

    <form method="post">
        <div dir="rtl" class="row">
            <div class="col-md-3">
                <a asp-route-culture="@Request.RouteValues["culture"]" data-toggle="modal" data-target="#prodModal" class="btn btn-info" name="CreateProduct">
                    @localizer.Get("مادة جديد") <i class="fas fa-plus"></i>

                </a>
            </div>
        </div>

        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div class="modal fade " id="prodModal" role="dialog" method="post" dir="rtl">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <div class="col-md-6">
                            <h3 class="modal-title text-info text-md-right"> اضافة مادة جديدة</h3>
                        </div>
                        <div class="col-md-6">
                            <a href="#" class="close float-md-left" data-dismiss="modal">&times;</a>
                        </div>
                    </div>
                    <div class="modal-body">
                        <div class="modal-body">

                            <div class="row form-group">
                                <div class="col-md-3">
                                    <label asp-for="ProdInfo.ProdCode"> </label>
                                </div>
                                <div class="col-md-5 ">
                                    <input asp-for="ProdInfo.ProdCode" class="form-control  text-md-right" />
                                </div>
                                <span asp-validation-for="ProdInfo.ProdCode" class="text-danger"></span>
                            </div>

                            <div class="row form-group">
                                <div class="col-md-3">
                                    <label asp-for="ProdInfo.ProdName"> </label>
                                </div>
                                <div class="col-md-5 ">
                                    <input asp-for="ProdInfo.ProdName" class="form-control  text-md-right" />
                                </div>
                                <span asp-validation-for="ProdInfo.ProdName" class="text-danger"></span>
                            </div>

                            <div class="row form-group">
                                <div class="col-md-3">
                                    <label asp-for="ProdInfo.CostPrice"> </label>
                                </div>
                                <div class="col-md-5 ">
                                    <input asp-for="ProdInfo.CostPrice" class="form-control  text-md-right" />
                                </div>
                                <span asp-validation-for="ProdInfo.CostPrice" class="text-danger"></span>
                            </div>

                            <div class="row form-group">
                                <div class="col-md-3">
                                    <label asp-for="ProdInfo.RetailPrice"> </label>
                                </div>
                                <div class="col-md-5">
                                    <input asp-for="ProdInfo.RetailPrice" class="form-control  text-md-right" />
                                </div>
                                <span asp-validation-for="ProdInfo.RetailPrice" class="text-danger"></span>
                            </div>
                            <div class="row form-group">
                                <div class="col-md-3">
                                    <label asp-for="ProdInfo.WholePrice"> </label>
                                </div>
                                <div class="col-md-5">
                                    <input asp-for="ProdInfo.WholePrice" class="form-control  text-md-right" />
                                </div>
                                <span asp-validation-for="ProdInfo.WholePrice" class="text-danger"></span>
                            </div>
                        </div>
                        <div class="modal-footer" dir="ltr">

                            <a href="#" class="btn btn-danger" data-dismiss="modal">الغاء</a>
                            @*<input asp-route-culture="@Request.RouteValues["culture"]" type="submit" class="btn btn-info" id="btnCreateProd" value="@localizer.Get("اضافة")" />*@
                            <input class="btn-default" type="button" id="btnCreateProd" value="Save" />
                        </div>

                    </div>

                </div>
            </div>
        </div>
    </form>


    <form method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div class="row form-group" dir="rtl">
            <div class="col-md-2">
                <label asp-for="@Model.WhId"> </label>
            </div>
            <div class="col-md-5 offset-md-5">
                <select asp-for="@Model.WhId"
                        asp-items="@(new SelectList(Model.WarehouseList, "Id", "WhName"))"
                        class="form-control ">
                </select>
            </div>

            <span asp-validation-for="@Model.WhId" class="text-danger"></span>
        </div>
        <table dir="rtl" id="dataTable" class=" text-md-center table table-striped border table-responsive-md table-responsive-lg">
            <tr>
                <th>
                    @Html.DisplayNameFor(a => Model.InG[0].ProdInfo.ProdCode)
                </th>
                <th>
                    @Html.DisplayNameFor(a => Model.InG[0].Qty)
                </th>
                <th>
                    @Html.DisplayNameFor(a => Model.InG[0].Note)
                </th>
                <th><a title="اضافة" href="#" id="addNew">   <i class="fas fa-plus"></i></a></th>
            </tr>
            @if (Model.InG != null && Model.InG.Count > 0)
            {

                int j = 0;

                foreach (var i in Model.InG)
                {


                    <tr>
                        @*float a= (float)((ArrayList)paredes[i])[0];*@
                        @*<td>@Html.TextBoxFor(a => a[j].ContactName)</td>*@

                        <td onclick="bindAutocomplete(this)">@Html.TextBoxFor(a => Model.InG[j].ProdInfo.ProdCode) </td>
                        <td>@Html.TextBoxFor(a => Model.InG[j].Qty)</td>
                        <td>@Html.TextBoxFor(a => Model.InG[j].Note)</td>
                        <td>
                            @if (j > 0)
                            {

                                // <input type="button" value="Delete" onclick="deleteRow(this)">
                                <a href="#" id="removeThis" class="remove">Remove</a>

                            }
                        </td>
                        <span asp-validation-for="@Model.InG[j].ProdInfo.ProdCode" class="text-danger"></span>
                        <span asp-validation-for="@Model.InG[j].Qty" class="text-danger"></span>
                    </tr>
                    j++;

                }

            }

        </table>

        <div class=" form-group row">

            <div class="col-md-7 offset-md-5">
                <div class="row">
                    <div class="col-md-6">
                        @*<input type="submit" class="btn btn-info form-control" value="حفظ" />*@
                        <input asp-route-culture="@Request.RouteValues["culture"]" type="submit" class="btn btn-info form-control" value="@localizer.Get("اضافة")" />

                    </div>
                    <div class="col-md-6">
                        @*<a asp-page="Index" class="btn btn-success form-control">الرجوع</a>*@

                        <a asp-page="Index" asp-route-culture="@Request.RouteValues["culture"]" class="btn btn-secondary form-control">
                            @localizer.Get("رجوع")
                        </a>

                    </div>

                </div>

            </div>


        </div>

        @*<input type="submit" value="Save Bulk Data" />*@

    </form>
    @*@using (Html.BeginForm("BulkData", "Save", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            @Html.ValidationSummary(true)

        }*@
</div>



<script>
    function RemoveRow(r) {
        var i = r.parentNode.parentNode.rowIndex;
        document.getElementById("dataTable").deleteRow(i);
    }

</script>

@* Here I will add Jquery Code for validation / dynamically add new rows / Remove rows etc *@

@section Scripts{
    <script language="javascript">
            $(document).ready(function () {

                //1. Add new row
                $("#addNew").click(function (e) {
                    e.preventDefault();
                    var $tableBody = $("#dataTable");
                    var $trLast = $tableBody.find("tr:last");

                    var $trNew = $trLast.clone();
                    $trNew.find(':text').val('');

                    var suffix = $trNew.find(':input:first').attr('name').match(/\d+/);
                    $trNew.find("td:last").html('<a href="#" id="removeThis" onclick="return RemoveRow(this)" class="remove" title="الغاء"><i class="fas fa-window-close"></i></a>');


                    $.each($trNew.find(':input'), function (i, val) {
                        // Replaced Name
                        var oldN = $(this).attr('name');
                        var newN = oldN.replace('[' + suffix + ']', '[' + (parseInt(suffix) + 1) + ']');
                        $(this).attr('name', newN);
                        //Replaced value
                        var type = $(this).attr('type');
                        if (type.toLowerCase() == "text") {
                            $(this).attr('value', '');
                        }

                        // If you have another Type then replace with default value
                        $(this).removeClass("input-validation-error");

                    });
                    $trLast.after($trNew);

                });



            });

            function bindAutocomplete(t) {
                $(t.children[0]).autocomplete({

                        source: '@Url.Page("Create", "SearchNow")'
                    });

            }

                  async function bindPrice(t) {
                      if (document.getElementById("PriceType").value == 1) {
                          const obj = await fetch(`${window.location.href}?handler=ProdUnitPriceWhole&term=${t.children[0].value}`)
                          const json = await obj.json()
                           t.parentElement.children[2].children[0].value = json[0]
                      }
                      else {
                                                     const obj = await fetch(`${window.location.href}?handler=ProdUnitPriceRetail&term=${t.children[0].value}`)
                          const json = await obj.json()
                           t.parentElement.children[2].children[0].value = json[0]
                      }


                       console.log(document.getElementById("PriceType").value)

                }

                function bindTotal(t) {
                    const val = t.children[0].value
                    const price = t.parentElement.children[2].children[0].value

                    t.parentElement.children[3].children[0].value = val * price
        }


    </script>

    <link rel="stylesheet" href="https://code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css">
    <script src="https://code.jquery.com/jquery-1.12.4.js"></script>
    <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>

    <script>
        $(document).ready(function () {

        });

        $(document).ready(function () {
            $(window).keydown(function (event) {
                if (event.keyCode == 13) {
                    event.preventDefault();
                    return false;
                }
            });
        });
    </script>
    @*<script>
            async function CreateProd() {
                const obj = await fetch(`${window.location.href}?handler=CreateProduct`)
                console.log("WORKS!");
            }

        </script>*@
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial");}

    @*<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>*@
    <script>
        $(document).ready(function () {
            $("#btnCreateProd").click(function () {
                //$.ajax(
                //    {
                //        type: "POST", //HTTP POST Method
                //        url: "/Create?handler=CreateProduct", // Controller/View
                //        data: { //Passing data
                //           // Name: $("#txtName").val(), //Reading text box values using Jquery
                //           // City: $("#txtAddress").val(),
                //           // Address: $("#txtcity").val()
                //        }

                //    });
                $.ajax({
                    type: "GET",
                    url: "/Create?handler=Test",
                    contentType: "application/json",
                    dataType: "json",
                    success: function (response) {
                        console.log("it works!");
                    },
                    failure: function (response) {
                        console.log("error");
                    }
                });
                console.log("Clicked");

            });
        });

    </script>
}


@*<form method="post">



        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <br />


        <div class="form-group row ">

            <div class="col-md-5 offset-md-5">
                <input type="text" id="ProdInfo" asp-for="ProdCode" class="form-control text-md-right" />
            </div>
            <div class="col-md-2 ">
                <label asp-for="IncomingGood.ProdInfo.ProdCode"></label>
            </div>
            <span asp-validation-for="ProdCode" class="text-danger"></span>
        </div>


        <div class="form-group row">

            <div class="col-md-5 offset-md-5">
                <select asp-for="WhId"
                        asp-items="@(new SelectList(Model.WarehouseList, "Id", "WhName"))"
                        class="form-control ">
                </select>
            </div>
            <div class="col-md-2">
                <label asp-for="IncomingGood.Warehouse.WhName"></label>
            </div>


            <span asp-validation-for="IncomingGood.Warehouse.WhName" class="text-danger"></span>
        </div>


        <div class="form-group row">

            <div class="col-md-5 offset-md-5">
                <input asp-for="Qty" id="qty" class="form-control  text-md-right" />
                <span asp-validation-for="Qty" class="text-danger"></span>

            </div>
            <div class="col-md-2">
                <label asp-for="Qty"></label>
            </div>
        </div>

        <div class="form-group row">

            <div class="col-md-5 offset-md-5">
                 <textarea placeholder="@localizer.Get("الملاحظات")" asp-for="Note" class="form-control  text-md-right"></textarea>

            </div>
            <span asp-validation-for="Note" class="text-danger"></span>
            <div class="col-md-2">
                <label asp-for="Note"></label>
            </div>
        </div>

        <div class="form-group row">
            <div class="col-md-5 offset-md-5">
                <div class="row">
                    <div class="col-md-6">
                         <a asp-page="Index" class="btn btn-success form-control" asp-route-culture="@Request.RouteValues["culture"]">  @localizer.Get("الغاء")</a>

                    </div>
                    <div class="col-md-6">
                         <button asp-route-culture="@Request.RouteValues["culture"]" type="submit" class="btn btn-info form-control" onclick="return ValidateInput()">  @localizer.Get("اضافة")</button>

                    </div>

                </div>
            </div>
        </div>


    </form>*@
</div>


@*@section Scripts {
        @{ await Html.RenderPartialAsync("_ValidationScriptsPartial");}

        <link rel="stylesheet" href="https://code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css">
        <script src="https://code.jquery.com/jquery-1.12.4.js"></script>
        <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>

        <script>
             $(document).ready(function () {
        $('#ProdInfo').autocomplete({
        source: '@Url.Page("Create", "SearchNow")'
        });
        });
        </script>

        <script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>
        <script>
            function ValidateInput() {
                var qty = document.getElementById("qty").value;
                var PId = document.getElementById("ProdInfo").value;

                if (qty == 0) {

                    swal("Error!", "No quantity Entered", "error");
                    return false
                }

            }

        </script>

    }*@
